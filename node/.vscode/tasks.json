{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build ROS2 workspace",
            "type": "shell",
            "command": "colcon build --packages-select demo_cpp_pkg",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
